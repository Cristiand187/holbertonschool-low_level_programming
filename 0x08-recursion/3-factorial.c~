#include "holberton.h"
#include <stdio.h>

/**
 * _strlen_recursion - This function is the print string pointer reverse.
 *
 * @s: String to printer
 *
 * Return: xxx
 */

int factorial(int n)
{
	if (n == 0)
		return (1);
	else if (n < 0)
		return (-1);
	else
		return (n*factorial(n-1));
}
