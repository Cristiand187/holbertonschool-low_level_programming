#include "holberton.h"
#include <stdio.h>

/**
 * set_string - this function that fills memory with a constant byte.
 *
 * @s: Parameter bytes of the memory area pointed.
 * @to: xx
 *
 * Return: Returns a pointer to the memory area
 *
 */

void set_string(char **s, char *to)
{
	*s = to;
}
